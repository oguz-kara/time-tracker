/**
 * Shared GraphQL scalar types
 * These are registered in the builder and can be used across all modules
 */

// DateTime scalar is already registered in builder.ts
// Add any additional custom scalars here
